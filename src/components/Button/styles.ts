import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '61%',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    gap: 7,
    backgroundColor: '#000',
    padding: 10,
    borderRadius: 16,
  },
  icon: {
    color: '#fff',
    fontSize: 20,
  },
  text: {
    color: '#fff',
    fontSize: 16,
  },
})
